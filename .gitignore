# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# ─── Segredos / Credenciais ─────────────────────────────────────────────────
# CRÍTICO: estes arquivos contêm chaves de API, hashes de senha e tokens.
# JAMAIS commitar em qualquer repositório (público ou privado).

# Firebase — chaves de API e IDs de app
lib/core/firebase_options.dart
android/app/google-services.json
ios/Runner/GoogleService-Info.plist
windows/flutter/firebase_options_windows.dart

# Configuração da aplicação — hash da senha master, pepper, constantes internas
lib/core/config/app_config.dart

# Variáveis de ambiente
.env
*.env
.env.*
!.env.example

# Keystore Android — certificado de assinatura do APK
*.jks
*.keystore
android/key.properties
android/app/upload-keystore.jks

# Certificados e chaves privadas genéricas
*.pem
*.p12
*.p8
*.cer
*.der
serviceAccount*.json
*service_account*.json
*credentials*.json

# ─── Documentos de pesquisa / dados sensíveis ────────────────────────────────
# Pasta com documentos do doutorado — podem conter dados de participantes.
"JORNADA DE COMHECIMENTO/"
JORNADA*/

# ─── IDE ─────────────────────────────────────────────────────────────────────
.vscode/
.idea/
